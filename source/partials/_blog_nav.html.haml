%h2 Articles 
%ul
  - page_articles.each_with_index do |article, i| 
    - next if article == current_page
    %li= link_to article.title, article

%h3 Tags
- blog.tags.each do |tag, articles|
  = link_to "#{tag}(#{articles.count})", "/blog/tags/#{tag.gsub(/\s+/,'-')}.html"